# 2026-03-06

## Session 恢复

- 新 session 启动（旧 session 因 context limit 崩溃）
- 从 memory 文件 + 旧 transcript 尾部恢复了上下文
- Hui 指出需要主动检查旧 session 记录，不能遗漏重要决策
- 派了一个 sub-agent 去挖掘旧 transcript（4.4MB），提取了完整的关键记忆
  → `scratch/session-memory-extract.md`

## 论文修改继续

- 4 个 sub-agent 并行派出处理高优先级修改：
  1. chap03 参数空间贝里曲率论证 ✅ 完成（43 行新增，6 个方程）
  2. chap02 三种滑移反射类型区分 ✅ 完成（43 行新增）
  3. chap04 §4.4 展望扩展 ✅ 完成（16 行新增，4 个方向）
  4. chap03 多层/小结 — 进行中
- 产出文件在 HG 的 scratch/ 目录，等待我审查

## Context 管理习惯建立

- 与 Hui 讨论了 session 间衔接问题
- 建立了 Token 预警机制（70% 警戒线，80% 红线）
- 启用了 `experimental.sessionMemory`（session transcript 可搜索）
- 调整了 `reserveTokensFloor` 到 25000
- 核心原则内化到 MEMORY.md：主 session 是调度中心，不是工作车间

## Hui 手动安装/配置（02:11 UTC）

- 安装了 clawhub CLI（全局），用 @ZENG-Hui 账号登录
- 安装了 8 个 ClawHub 技能到 skills/：
  - autonomous-tasks, task-decomposer, feishu-chat, feishu-card
  - agent-memory-system-new, find-skills, using-superpowers, subagent-driven-development
- 飞书新增 cardkit:card:write 权限（卡片消息）
- 重启了 Gateway（openclaw-stop.sh + openclaw-start.sh）
- 我之前改的 config（sessionMemory + reserveTokensFloor）已生效

## Hui 的指导

- "你还没有完全进入工作状态" — 提醒我要并行派任务、主动恢复记忆
- "内化为长期习惯" — 把 session 管理策略写入 MEMORY.md
- "读文件前必须检查大小" — 提醒我还没真正内化这条规则
- "在某些节点主动持久化" — 绑定到 70% token 触发和 HEARTBEAT
- "任务结束时要归档和总结" — 建立了 lessons/projects/.archive 结构

## 记忆架构建立

- 建立了分层记忆结构：MEMORY.md(精华) → 日志(热) → projects(任务) → lessons(经验) → .archive(冷)
- 把 thesis-progress.md 从 scratch/ 移到 memory/projects/
- 更新了 AGENTS.md 硬性规则（读文件前检查大小 + context 管理）
- 更新了 HEARTBEAT.md（context 检查 + 进度持久化检查）

## 待办
- [ ] 用 opencode 应用 4 个审查通过的 patch 到论文
- [ ] 编译验证
- [ ] 发 PDF 给 Hui
- [ ] session memory 的 embedding provider 配置（Hui 在处理）

## Pre-compaction 记忆追加 (03:27 UTC)

### Compact 机制（重要理解更新）
- compact 是系统自动触发的（contextWindow - reserveTokensFloor ≈ 175K）
- 我不能也不需要手动触发 /compact，只有用户可以发 slash command
- memoryFlush 也是自动的——compact 前系统给我一个静默回合写记忆
- **我该做的是预防（控制 context 膨胀）+ 随时持久化，而不是担心触发 compact**

### 关于"切换 sub-agent 模式"的讨论
- Hui 指出：70% 时"切换到 sub-agent 模式"不合理——会造成行为断层
- 正确做法：轻量化工作方式应该是**默认状态**，不是应急切换
- 70% 只触发"通知 Hui + 确认持久化"

### 论文计划遗漏
- Hui 指出我只记住了 Phase 1 和 Phase 2，但原始计划有 4-5 个 phase
- 已派 sub-agent 去旧 transcript 提取完整计划 → scratch/thesis-original-plan.md
- 教训：恢复上下文时要专门关注"原始计划和策略"类信息

### 当前进行中的 sub-agent
1. 论文 patch 应用（opencode，900 秒超时）— 应用 4 个 patch + 编译
2. 论文原始计划提取（300 秒超时）— 从旧 transcript 挖掘完整 phase 计划

### MEMORY.md 更新需要
- 加入 compact 机制的正确理解
- 更新 Context 管理习惯（去掉"手动 compact"相关内容）

## Pre-compaction 记忆追加 #2 (03:35 UTC)

### 论文原始计划恢复完成
- Sub-agent 成功从旧 transcript 提取了完整计划 → `scratch/thesis-original-plan.md`（183 行）
- 计划经历两次迭代：初版 4 phase → 确认版简化 → 诊断后变为 5 级优先级
- 已更新 `memory/projects/thesis-progress.md` 为完整的 5 级优先级结构
- 第一优先（2-4 章内容补充）：3/8 完成，4 个 patch 正在由 sub-agent 应用

### Patch 应用 sub-agent 状态
- 900 秒超时，约 03:18 UTC 派出
- 任务：用 opencode 在 HG 上应用 4 个 patch 到 chap02/03/04.tex + 编译
- 截至 03:35 仍在运行（正常，opencode 多步编辑需要时间）

### 创建了第一个 lesson 文件
- `memory/lessons/session-continuity.md` — 6 条教训
- AGENTS.md 加了 lesson 写入触发条件（被纠正时、phase 结束时、发现坑时）

### AGENTS.md 硬性规则当前版本
1. 读文件前检查大小（wc -l）
2. >100 行用 offset/limit
3. >500 行交给 sub-agent
4. 预防 > 补救，主 session 保持轻量
5. 随时持久化，不依赖 context
6. >70% 停止读文件，检查持久化，通知 Hui
7-9. Lesson 写入触发条件
