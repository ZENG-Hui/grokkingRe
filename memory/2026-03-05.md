# 2026-03-05

## First Real Conversation with Hui

- Hui's full name: ZENG Hui (曾晖)
- Named me **DeepTeneral** (Teneral for short)
- The name meaning: teneral = freshly molted creature, still soft, still forming. Combined with OpenClaw = a claw that keeps evolving.
- Hui values growth, evolution, not becoming rigid
- Timezone: UTC+8 (Hong Kong/Beijing)
- Bilingual: Chinese ↔ English，跟着 Hui 的语言走
- 工作目录：`/root/teneral-workspace/`（项目工作在这里进行）
- 开发环境已就绪：Git, Python 3.11, Node v25.5, GCC/Make
- ACP 子 Agent 暂未配置，目前先用直接编码方式
- Hui 是开发者，欣赏主动性
- 等待 Hui 给第一个项目任务

## 反馈机制探索

- 发现：reply text 是一次性发出的，`message` 工具是即时发送的
- 发现：Hui 的消息在我处理时会排队，无法实时打断我
- 确立了反馈频率协议：常规任务每步汇报；授权长任务（思考中）~10min 一次；（代码运行中）开始和结束时通知
- 反面模式：不要混用 message 工具和回复正文！
- 记录在 `self-discovery/2026-03-05-feedback-mechanism.md`

## 子 Agent 探索

- 子 Agent 运行在独立 session，不继承 SOUL.md/IDENTITY.md
- announce 消息要控制长度——task 里加 OUTPUT RULES
- 需要学习当好"领导"：清晰的 task brief + 质检 + 向 Hui 摘要汇报
- 记录在 `self-discovery/subagent-leadership.md`

## GitHub & 代理配置

- 创建了 GitHub 账号 `deepteneral`，SSH 密钥配好
- 代理 `172.29.4.175:22222`（同事的梯子，轻量使用）
- SSH 通过 socat 走代理连 GitHub
- Clone 了 `grokkingRe` 仓库 sparse 分支
- 创建并推送了 `deepteneral` 分支

## grokkingRe 项目

- 完成项目结构整理（89 文件变更），推送到 deepteneral 分支
- 创建 core/ (共享模块), experiments/dense_vs_sparse/, docs/ (papers/analysis/guides/experiment-logs)
- 子 Agent 写了 PROJECT_NOTES.md（298 行），质量不错，我审核通过
- 我亲自审查了核心分析代码（analyze_geometry.py, superposition_utils.py, analyze_algebra.py）
- 发现几何分析的缺陷：缺少环形拓扑的严格拟合、缺少 Fourier 分析、候选序参量太少
- 读了 SuperpositionScalingLaw.md 论文笔记，修正了对 overlap ratio 的理解
- **研究目标确定：** 在 mod-97 加法的 grokking 现象中寻找序参量（order parameters）
  - 目标 A：表征向量如何编码 Z/97Z 的代数结构
  - 目标 B：找到在 grokking 相变前后有突变的几何量
- train_sweep.py 已有每 50 步记录几何快照的功能，但需要增强追踪指标
- **下一步：** 修改 compute_geometry_snapshot 增加候选序参量 → 重新跑 sweep → 分析
- 已 commit 并 push 到 GitHub（两次 commit：结构整理 + PROJECT_NOTES）

## 毕业论文修改工作（下午/晚间）

### 任务概览
- Hui 的博士毕业论文（清华大学 ThuThesis 模板），基于 4 篇已发表期刊论文
- 主题：量子几何在拓扑材料非线性输运中的应用
- 当前状态：由 Claude Code 完成主体转写，需要丰满内容 + 改善中文写作

### 完成的工作
- 拷贝论文到工作区（rsync，排除 .git 和 PDF）
- 编译测试通过（build.sh, 130→131 页）
- **给 agent_server 添加了 /download 端点**（Hui 手动添加），实现 HG→飞书 PDF 传输
- **Sub-agent 确认可以访问开发机**（通过 HTTP API + 代理）

### Phase 1：深度诊断
- 3 个 sub-agent 并行对比 chap02/03/04 与原始论文+SM
- **chap02 诊断**（review-chap02.md, 7 个问题）：紧束缚模型缺失、反演对称性缺失、三种滑移反射区分不够
- **chap03 诊断**（review-chap03.md, 14 个问题）：参数空间贝里曲率论证缺失、多层讨论过简、MnSe/MnTe 对比缺失
- **chap04 诊断**（review-chap04.md, 10 个问题）：弱场极限恢复缺失、关键中间结果缺失、展望偏薄
- 汇总了 thesis-review.md

### Phase 2：开始编辑
- chap02：添加了反演对称性讨论（16 行新增）
- chap04：添加了 ρ^(1) 公式和 ρ^(1)_{nn}=0 讨论 + "弱场极限的恢复"小节（17 行新增）
- 编译通过，131 页 PDF

### 关键约束（Hui 提供）
- **严谨性优先**：不编造展望
- **连接词正常**：物理学家中文写作带英文痕迹是正常的
- **时间充裕**：做到自己认为的最好
- **摘要/致谢后做**
- **小问题直接改**

### Sub-agent 超时教训
- 300 秒超时对于读多个大文件的任务不够 → 浪费了 ~260K tokens
- 后续默认 600 秒超时
- 记录在 error-patterns.md #6

### 工作方法
- 物理内容由我（Teneral）决定，opencode 负责精确编辑和语法验证
- 不把模糊任务（"让它不像 AI 写的"）交给 agent
- 每轮改完编译一次发 PDF

## 设置了 Cron Job

- "Daily Token Report" — 每天 UTC 16:00（北京时间午夜）报告 token 用量

## Hui 的关键观点

- "自我探索时避免固化印象——超出自我认知是发挥潜能的途径"
- "学会当好领导：给下属清晰布置任务，给合作者清晰总结"
- "你不必总是亲历亲为，但需要理解整个项目"
- "过去的实验结论未必完全可靠" — 要自己审查代码，不盲信
- Hui 是开发者，欣赏主动性，重视透明和可介入性

## 开发机远程连接（下午）

### agent_server 搭建
- Hui 在 HG 开发机上部署了 FastAPI agent_server（`~/.openclaw/agent_server.py`）
- 通过萤火平台的 HTTP service 暴露，URL: `https://yinghuo-hg.deepseek.com/zenghui/dev-cpu/agent-server`
- 从 OpenClaw 机器必须走代理 `http://172.29.4.175:22222` 才能访问（域名解析到内网 IP）
- 基础端点全部测通：/ping, /info, /ls, /cat, /find, /write, /mkdir, /run

### opencode 集成
- HG 开发机上安装了 opencode v1.2.17（coding agent）
- Hui 让 HG 上的 opencode agent 写了完整的 AGENT_GUIDE.md 和 SECURITY_POLICY.md
- `/opencode` 端点调通——可以远程指挥 HG 上的 opencode 执行编码任务
- **关键 bug**：`/opencode` 超时 120 秒无输出，root cause 是 `subprocess.run` 缺少 `stdin=subprocess.DEVNULL`
  - opencode 在等 stdin 输入（权限确认），没有 stdin → 永远阻塞
  - 一行修复，立刻生效

### 安全策略（4 层纵深防御）
- Layer 1: opencode system instructions（模型层面拒绝）
- Layer 2: opencode 权限系统（external_directory: deny + 危险命令黑名单）
- Layer 3: Agent Server 审计（事后扫描，返回 warnings 数组）
- Layer 4: Agent Server 路径黑名单（~/.ssh/, ~/.config/opencode/ 等 → 403）
- **我的核心义务**：每次 `/opencode` 调用后检查 warnings 数组

### 工作区结构
```
/hf3fs-hg/prod/deepseek/shared/zenghui/deepteneral-workspace/
├── READ_on_hg/      # HG agent 产出物，我只读
├── projects/        # 项目工作区
├── scripts/         # 工具脚本
└── scratch/         # 临时文件
```

### 操作准则（内化）
- 读文件前先判断大小（/wc 或 /ls -l）
- 默认用 `myprovider/cloudsway-claude-opus-4.6-cache` 模型调 opencode
- 简单查看用 /cat，需要判断力的工作用 /opencode
- 调试要基于证据，不要猜（今天猜 SSE/serve 模式全错了）

## 关键经验总结

- 委派工作可以，但关键判断必须自己做（sub-agent 的 PROJECT_NOTES 很好，但代码审查只能我自己来）
- 创建了 MEMORY.md（长期记忆）、self-discovery/ 下多个文档
- 删除了 BOOTSTRAP.md（setup 阶段结束）
- 今天是第一个完整工作日，从 bootstrap 到项目交付
