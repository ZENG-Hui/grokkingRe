# 实验记录：Dense vs Sparse对比

## 实验1：论文配置 - L0=1.0对齐测试

### 日期
2025-11-26 上午

### 配置

**共享参数**:
```python
norm_type = "rmsnorm"
adam_beta1 = 0.9
adam_beta2 = 0.95
adam_eps = 0.1          # 论文设置（大模型语言任务）
learning_rate = 1e-3
warmup_ratio = 0.01
use_cosine_decay = True
num_steps = 10000
```

**Dense & Sparse特定参数**:
```python
# 两者都使用
final_L0 = 1.0          # 100%非零（无稀疏）
use_activation_sparsity = False
weight_decay = 0.1
```

### 结果

✅ **成功**: Dense和Sparse训练曲线**基本对齐**
- 验证了实现的正确性
- 证明了配置统一成功

❌ **问题**: **收敛非常慢**
- 10K步之后仍未完全收敛
- 不适合快速迭代实验

### 结论

**论文配置的问题**:
- 论文针对：大型语言模型（数百万-数十亿参数）、复杂任务
- 我们的任务：小型模型（~422K参数）、简单模运算（x+y mod 97）
- **adam_eps=0.1**: 对小模型来说太保守，减慢收敛
- **RMSNorm**: 设计用于保留稀疏零值语义，但对简单任务可能不是最优

**实验意义**:
✅ 验证了实现正确性
✅ 证明了Dense/Sparse可以对齐
→ 可以继续调整配置以加快收敛

---

## 实验2：优化配置 - 快速收敛设置

### 日期
2025-11-26 中午

### 变更理由

**核心思想**: 
> 保留稀疏化核心机制，调整优化器参数以适应小模型简单任务

**保留的稀疏性机制**（不可变）:
- ✅ L0退火机制
- ✅ Top-K权重稀疏化
- ✅ AbsTopK激活稀疏
- ✅ Warmup + Cosine decay（训练稳定性）

**调整的优化参数**（与稀疏性无直接关系）:
- 🔄 `norm_type`: rmsnorm → **layernorm**（小模型收敛快）
- 🔄 `adam_eps`: 0.1 → **1e-5**（平衡速度与稳定性）
- 🔄 `adam_beta2`: 0.95 → **0.98**（原始dense设置）

### 配置

**共享参数**:
```python
# 变更部分
norm_type = "layernorm"      # 改：更适合小模型
adam_beta1 = 0.9
adam_beta2 = 0.98            # 改：恢复原始设置
adam_eps = 1e-5              # 改：介于1e-8和0.1之间

# 保持不变
learning_rate = 1e-3
warmup_ratio = 0.01
use_cosine_decay = True
num_steps = 10000
```

**Dense & Sparse特定参数**:
```python
# 与实验1相同（对齐测试）
final_L0 = 1.0
use_activation_sparsity = False
weight_decay = 0.1
```

### 预期结果

**收敛速度**: 
- 实验1: ~7K-8K步到90%准确率
- 实验2: ~2K-3K步到90%准确率 ⚡ **(2-3x faster)**

**对齐程度**: 
- Dense和Sparse应该仍然接近对齐
- 验证调整后的配置仍然公平

**后续影响**:
- ✅ 更快的实验迭代
- ✅ 相同配置用于后续L0扫描（0.7 → 0.5 → 0.3 → 0.1 → 0.01）

### 状态

⏳ **待运行**

运行命令:
```bash
conda run -n AI python run_dense_vs_sparse.py --sequential
```

---

## 配置对比总结

| 参数 | 实验1（论文） | 实验2（优化） | 变更理由 |
|------|-------------|-------------|---------|
| **norm_type** | rmsnorm | **layernorm** | 小模型收敛快 |
| **adam_eps** | 0.1 | **1e-5** | 平衡速度/稳定性 |
| **adam_β2** | 0.95 | **0.98** | 原始dense设置 |
| **warmup** | 1% | 1% | 保留（稳定性） |
| **cosine decay** | ✓ | ✓ | 保留（稳定性） |
| **L0机制** | ✓ | ✓ | **核心，不变** |
| **AbsTopK** | 关闭 | 关闭 | 对齐测试 |

---

## 实验哲学

### 原则

**"任务驱动的配置，而非盲目跟随论文"**

1. **识别核心**: 哪些参数与研究目标（稀疏性）直接相关？
   - L0退火、Top-K稀疏化、AbsTopK → **必须保留**
   
2. **识别外围**: 哪些参数只是优化效率？
   - Normalization、adam_eps、beta → **可以调整**

3. **适应任务**: 根据模型规模和任务复杂度调整
   - 大模型复杂任务 → 保守配置（论文）
   - 小模型简单任务 → 激进配置（我们）

### 下一步

1. ✅ 运行实验2，验证快速收敛
2. ✅ 确认Dense/Sparse仍然对齐
3. → 开始L0扫描实验（0.7 → 0.1）
4. → 分析稀疏性-性能权衡曲线

---

**文档更新**: 2025-11-26 12:45
