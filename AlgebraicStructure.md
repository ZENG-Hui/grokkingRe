# Transformer 如何表征和计算 mod 97 加法

## 问题

我们训练了一个 2 层 Transformer 做 `x + y (mod 97)`，训练后 val_acc = 100%。我们想知道：

1. 模型的权重几何中编码了什么数学结构？
2. 单位元（x+0=x）和逆元（x+(97-x)=0）的知识存储在哪里？
3. 计算在哪一层发生？

我们从三个层次回答这些问题：**静态权重分析**（各层的权重矩阵编码了什么 token 间关系）、**全层投影分析**（每层如何变换 token 的表征）、**动态激活分析**（实际计算时各层的信号如何变化）。

## 分析一：Output 层的静态权重几何

### 方法

Output projection 层（output_proj，形状 [99, 128]）将 128 维隐藏状态映射到 99 个 token 的 logits。前 97 行分别是 0-96 这 97 个输出 token 的表征向量。我们计算 97x97 的余弦相似度矩阵，检验其中的结构模式。

### 发现一：模型把 97 个 token 排成一个"环"

最强的信号是**数值邻近性**：数值上相邻的 token 有更高的余弦相似度。

| token 间的数值距离 | Dense wd=1 | Sparse L0=0.40 | Dense wd=0.3 |
|---|---|---|---|
| 1（如 token 32 和 33） | **0.148** | **0.112** | **0.070** |
| 2 | 0.031 | 0.043 | 0.040 |
| 5 | -0.016 | 0.030 | 0.000 |
| 10 | 0.030 | 0.007 | -0.022 |
| 48（"对径"，最远） | **-0.082** | **-0.062** | -0.023 |

所有模型都呈现同样的模式：距离 1 时相似度最高，距离 48（圆环的另一端）时相似度为负。

**物理直觉**：想象 0-96 这 97 个点均匀排列在一个圆环上。相邻的点被映射到表征空间中相近的方向，对径的点被映射到相反的方向。模型在 128 维空间中"画"了一个（大致的）圆。

**为什么这是合理的**：对于加法 mod 97，当 x 或 y 变化 1 时，输出 (x+y) mod 97 也变化 1（除了在 96→0 的回绕处）。所以数值相近的输出 token 确实出现在相似的上下文中，应该有相似的表征。

### 不同模型的差异

- **wd=1**：环形结构最紧凑（邻居相似度 0.148，对径反相关 -0.082）。高 weight decay 迫使模型压缩表征，放大了环形结构。
- **wd=0.3**：环形结构较弱（邻居 0.070，对径 -0.023）。低 weight decay 允许使用更多维度，不需要那么紧凑的布局。
- **Sparse L0=0.40**：环形结构保持良好（0.112），同时 overlap ratio 最低（3.79）。稀疏化去掉了冗余连接，但保留了功能性的环形编码。

### 发现二：离散代数结构未被编码

我们测试了三种 Z/97Z 的数学结构，**全部没有显著信号**：

### 加法逆元：a 与 97-a

例如 token 3 和 token 94 相加等于 97 ≡ 0 (mod 97)。我们可能期望模型认为它们"相关"。

| 模型 | 逆元对的平均相似度 | 所有对的平均相似度 | 信号强度 |
|---|---|---|---|
| Dense wd=1 | 0.014 | 0.002 | 0.16 个标准差 |
| Sparse L0=0.40 | 0.004 | -0.001 | 0.04 个标准差 |

**结论**：无信号。逆元对和随机 token 对没有区别。这其实可以理解——output 层只需要"输出正确答案"，不需要编码输入之间的关系。加法逆元关系应该在处理输入的 embedding 或 attention 层才会体现。

### 乘法群结构

Z/97Z 的非零元素 {1, ..., 96} 构成一个乘法群。这个群有丰富的子结构——比如可以把元素分成"平方数"和"非平方数"两类（类似于正负号），或者按照"对数"来排序（类似于取对数后在实数轴上排列）。

我们测试了这两种分类方式：

**平方数 vs 非平方数**：将 1-96 分成两组各 48 个元素。组内相似度和组间相似度完全相同（差异 < 0.003），没有任何信号。

**对数分组**：选定一个"基"（g=5），每个非零元素都可以写成 5 的某个幂次。按幂次的奇偶（或者 mod 4）分组。同样没有信号——组内和组间平均相似度在 ±0.006 以内。

**结论**：乘法群结构对加法任务完全无关，模型没有学到任何乘法结构。这是意料之中的——如果我们改成做 x*y mod 97 的任务，预期会在几何中看到这些结构出现。

### 发现三：最相似/最不相似 token 对

### 最相似的 token 对以邻居为主

Dense wd=1 模型中，Top 20 最相似 token 对里有 8 对是距离-1 邻居：(32,33), (45,46), (63,64), (95,96), (39,40), (50,51), (64,65), (1,2) 等。

### 一个跨模型稳定的"最不相似"对

Token 对 (29, 65) 在所有模型中都是最不相似的 token 对之一：

| 模型 | cos_sim(29, 65) | 排名 |
|---|---|---|
| Dense wd=1 | -0.280 | 第 1 |
| Sparse L0=0.40 | -0.317 | 第 1 |
| Dense wd=0.3 | -0.235 | 第 9 |
| Sparse L0=0.60 wd=0.3 | -0.263 | 第 4 |

29 和 65 的数值距离是 36（或者从另一方向数是 61），不是最远的 48，但模型一致地把它们推到最不相似的位置。这可能与训练/验证集的具体划分有关——某些 (x,y) 对出现在训练集中而另一些不出现，导致某些输出 token 的上下文差异被放大。

### Token 0 并不特殊

0 是加法的单位元（x + 0 = x），但在 output 层没有任何特殊地位：范数接近其他 token 的平均值，与其他 token 的相似度分布也没有异常。这是合理的——作为加法的输出，0 出现的频率和其他 token 一样。

### 小结

| 测试的结构 | 是否存在 | 物理直觉 |
|---|---|---|
| 数值邻近性（圆环拓扑） | **强信号** | 加法的输出随输入连续变化，相邻答案应有相似表征 |
| 环形反相关（对径为负） | **存在** | 圆环上最远的点方向相反 |
| 加法逆元 (a, 97-a) | 无信号 | Output 层不编码输入间的关系 |
| 乘法群结构 | 无信号 | 与加法任务无关 |
| Token 0 特殊性 | 无 | 0 作为输出不特殊 |

## 分析二：全层投影——各层如何变换 token 表征

上面只分析了 output_proj（"刻度盘"），那模型的中间层（"指针机构"）编码了什么？

**方法**：中间层的权重矩阵（如 attention 的 Q/K/V 投影）行对应隐藏维度，不直接对应 token。但我们可以把每个 token 的 embedding 向量通过这些投影矩阵变换，得到每个 token 在该层空间中的表征向量，然后分析它们之间的余弦相似度结构。

### Dense 模型 (wd=1) 全层结果

| 层 | d=1 相似度 | d=48 相似度 | 逆元信号 | 说明 |
|---|---|---|---|---|
| token_embedding | 0.148 | -0.082 | 0.14σ | 输入层的环形编码 |
| Q_block0 | 0.140 | -0.034 | 0.20σ | Query 保留环形结构 |
| **K_block0** | **0.553** | **-0.534** | -0.58σ | **极强的环形结构！** |
| **V_block0** | **0.434** | **-0.284** | 0.50σ | **强环形 + 弱逆元信号** |
| attn_O_block0 | 0.155 | -0.081 | 0.11σ | Attention 输出，结构减弱 |
| FFN_up_block0 | 0.321 | -0.287 | -0.20σ | FFN 内部，中等环形 |
| Q_block1 | 0.112 | -0.113 | 0.11σ | 第二层 Query |
| **K_block1** | **0.518** | **-0.416** | -0.46σ | **再次出现极强环形** |
| **V_block1** | **0.421** | **-0.187** | 0.21σ | **再次强环形** |
| attn_O_block1 | 0.144 | -0.082 | 0.00σ | 输出，结构减弱 |
| FFN_up_block1 | 0.150 | -0.120 | -0.22σ | |
| output_proj | 0.148 | -0.082 | 0.14σ | 与 embedding 相同 |

### Sparse 模型 (L0=0.40, wd=1) 全层结果

| 层 | d=1 相似度 | d=48 相似度 | 逆元信号 | 说明 |
|---|---|---|---|---|
| token_embedding | 0.112 | -0.062 | 0.06σ | |
| Q_block0 | 0.151 | -0.072 | 0.10σ | |
| **K_block0** | **0.388** | **-0.402** | -0.24σ | **强环形** |
| **V_block0** | **0.369** | **-0.302** | -0.05σ | **强环形** |
| attn_O_block0 | 0.114 | -0.056 | 0.21σ | |
| FFN_up_block0 | 0.045 | -0.062 | 0.07σ | 稀疏化后 FFN 环形消失 |
| Q_block1 | 0.053 | -0.106 | 0.17σ | |
| K_block1 | 0.070 | -0.081 | -0.23σ | 稀疏化削弱了 block1 的 K |
| V_block1 | 0.114 | -0.071 | 0.14σ | |
| output_proj | 0.112 | -0.062 | 0.06σ | |

### 发现

**1. Key 层是环形结构的放大器。**

K_block0 的邻居相似度达到 0.553（Dense）和 0.388（Sparse），是 embedding 层（0.148 / 0.112）的 3-4 倍。对径反相关也放大到 -0.534 / -0.402。这意味着 **Key 投影矩阵把 embedding 中微弱的环形拓扑放大成强烈的"圆环信号"**。

物理类比：embedding 层像是一个模糊的圆环，Key 投影像一个透镜，把圆环聚焦得更清晰。

**2. Value 层也放大环形，但方式不同。**

V 层的邻居相似度（0.434 / 0.369）很高，但对径反相关（-0.284 / -0.302）不如 K 层极端。V 层的圆环更"平滑"，而 K 层的圆环更"尖锐"。这与它们的功能一致：K 决定"注意什么"（需要精确的位置分辨），V 决定"传递什么信息"（需要平滑的值编码）。

**3. Attention 输出层（attn_O）"混合"后结构减弱。**

每个 attention block 的输出（attn_O）的环形信号都回落到 embedding 水平（d=1 sim ≈ 0.14-0.15）。这是因为多头 attention 的加权求和混合了来自不同 token 的信息，稀释了单个 token 的环形位置信号。

**4. 稀疏化选择性地削弱了某些层的环形结构。**

Dense 模型中 FFN_up_block0 有中等强度的环形（d1=0.321），但 Sparse L0=0.40 中几乎消失（d1=0.045）。K_block1 也从 0.518 降到 0.070。这说明稀疏化不是均匀削弱所有层——它保留了 **block0 的 K 和 V**（核心计算层），但压缩了 block1 和 FFN 的环形冗余。

**5. 加法逆元和乘法结构在所有层都没有显著信号。**

所有层的逆元信号强度都 < 0.6σ，远低于统计显著性。乘法群结构（QR_diff 列）也全部在 ±0.04 以内。这说明模型在任何层都没有学到这些离散代数关系——它完全依赖连续的环形拓扑来解决加法任务。

**6. token_embedding 和 output_proj 的值完全相同。**

这不是巧合——检查后发现模型使用了 **weight tying**（输入 embedding 和 output 投影共享同一个权重矩阵）。这意味着输入表征和输出解码使用同一组向量，所以 output_proj 中的环形结构就是 embedding 中的环形结构。

### 静态分析的物理图景

综合分析一和分析二，模型的**静态结构**是：

1. **Embedding 层**：在 128 维空间中画了一个模糊的圆环（邻居 cos_sim ≈ 0.15）
2. **Key 投影**：像透镜一样把圆环聚焦得更清晰（邻居 cos_sim 放大到 0.55）
3. **Value 投影**：也放大圆环，但更平滑（0.43），适合传递连续值
4. **Output 层**：与 embedding 共享权重，用同一个圆环读出答案

Overlap ratio >> 1 的原因：97 个 token 的表征向量被组织在一个低维流形（圆环 ≈ 2 维）上，而非均匀散布在 128 维空间中。

但静态分析有一个根本局限：它无法解释模型如何**计算**。x+0=x 和 3+94=0 的知识编码在哪里？静态权重中找不到逆元和单位元的信号。这引出下一步——激活分析。

## 分析三：动态激活——计算在哪里发生

前两个分析看的是"仪器的刻度和透镜长什么样"。要回答"模型怎么算出 x+0=x"，需要看**实际输入通过模型时各层的信号**——"指针怎么运动"。

**方法**：给模型喂入实际的输入序列 `[x, op, y, eq]`，记录 `eq` 位置（最后一个 token）在每层输出后的 128 维隐藏状态向量，然后比较不同输入下的激活模式。

### 单位元（y=0）

比较 97 个 `[x, +, 0, =]` 输入和 200 个随机 `[x, +, y, =]`（y≠0）输入在 `=` 位置的激活：

| 层 | y=0 范数 | y≠0 范数 | 范数比 | cos_sim(y=0, y≠0) |
|---|---|---|---|---|
| after_embed | 2.039 | 2.039 | 1.000 | 1.000 |
| after_block0 | 1.419 | 1.417 | 1.002 | 0.997 |
| **after_block1** | **1.600** | **1.603** | **0.998** | **0.016** |
| after_final_norm | 7.588 | 7.586 | 1.000 | 0.018 |
| logits | 39.32 | 39.29 | 1.001 | 0.004 |

**关键发现**：

- **Block 0 之后（cos_sim=0.997）**：y=0 和 y≠0 的 `=` 位置激活几乎相同。Block 0 还没有区分 y 的值。
- **Block 1 之后（cos_sim=0.016≈0）**：y=0 和 y≠0 的激活变得**几乎正交**！第二层 Transformer 彻底改变了隐藏状态的方向，使其编码了具体的答案。
- **范数几乎不变**（比值都接近 1.0）：区分不同答案的信息编码在方向上，不是大小上。

这意味着 **block 1 是执行加法计算的核心层**。block 0 做的是"准备工作"（聚焦环形结构、建立 attention 模式），block 1 根据 x 和 y 的具体值将隐藏状态旋转到正确的方向。

模型的 identity accuracy（x+0=x）：**97/97 = 100%**。

### 逆元（x + (97-x) ≡ 0）

比较 96 个 `[x, +, 97-x, =]`（答案都是 0）和 200 个随机输入（答案≠0）：

| 层 | 逆元 norm | 其他 norm | 比值 | 逆元方差 | 其他方差 |
|---|---|---|---|---|---|
| after_embed | 2.039 | 2.039 | 1.000 | 0.000 | 0.000 |
| after_block0 | 1.417 | 1.417 | 1.000 | 0.0001 | 0.0001 |
| **after_block1** | 1.600 | 1.601 | 1.000 | **0.0004** | **0.0200** |
| **after_final_norm** | 7.623 | 7.584 | **1.005** | **0.009** | **0.449** |
| **logits** | 40.07 | 39.26 | **1.021** | **0.095** | **15.55** |

**关键发现：逆元对的激活高度收敛。**

虽然 96 个逆元对的输入完全不同（(1,96), (2,95), (3,94), ...），但它们在 block 1 之后的 `=` 位置激活**几乎相同**：
- 逆元对的方差 = 0.0004，普通输入的方差 = 0.0200（**低 50 倍**）
- 在 final_norm 后更明显：0.009 vs 0.449（**低 50 倍**）

收敛性测试确认了这一点：

| 层 | 逆元对内的 mean cos_sim | 随机输入对内的 mean cos_sim | 判定 |
|---|---|---|---|
| after_block0 | 0.992 | 0.993 | 无信号（block0 还没区分） |
| **after_block1** | **0.979** | **-0.000** | **强收敛！** |
| **after_final_norm** | **0.980** | **0.000** | **强收敛！** |

所有答案 = 0 的输入，在 block 1 之后的隐藏状态余弦相似度高达 0.98——它们被映射到了 128 维空间中几乎相同的方向。而随机输入对的余弦相似度为 0，说明不同答案的隐藏状态在空间中均匀散开。

模型的 inverse accuracy（x+(97-x)=0）：**96/96 = 100%**。

### 答案编码

对所有 97 个可能的答案，每个答案取 5 组不同的 (x, y) 输入，分析 `=` 位置在 final_norm 后的隐藏状态：

| 指标 | 值 |
|---|---|
| 同答案内 mean cos_sim | **0.980** |
| 不同答案间 mean cos_sim | **-0.009** |
| 比值 | ~109x |

**同一答案的不同输入产生几乎相同的隐藏状态**（cos_sim=0.98），而**不同答案的隐藏状态几乎正交**（cos_sim≈0）。模型在 block 1 完成计算后，将 128 维隐藏状态精确地旋转到一个"答案方向"，这个方向只取决于 (x+y) mod 97 的值，而不是 x 和 y 的具体组合。

答案方向之间的邻居结构：

| 答案间距 | mean cos_sim |
|---|---|
| 1 | 0.393 |
| 2 | 0.117 |
| 5 | -0.225 |
| 10 | 0.088 |
| 48 | -0.221 |

圆环拓扑再次出现！但这次信号比静态权重分析中强得多（距离 1 的相似度从 0.148 升到 0.393）。这是因为激活分析捕捉了整个计算流程的最终结果，而静态权重分析只看了单层。

### 全景：从输入到输出

```
输入 [x, +, y, =]
    ↓
Embedding + Position    "=" 位置的激活对所有输入都几乎相同
    ↓                   （cos_sim ≈ 1.0，还没开始计算）
Block 0                  开始分化但仍然相似
    ↓                   （cos_sim ≈ 0.997）
Block 1                  ★ 计算发生在这里 ★
    ↓                   隐藏状态被旋转到"答案方向"
                        同答案的不同输入 → cos_sim = 0.98
                        不同答案的输入 → cos_sim ≈ 0
    ↓
Final Norm + Output     读出答案，accuracy = 100%
```

**单位元的知识不是通过特殊的"0 检测器"实现的**——模型只是在 block 1 中统一地计算 (x+y) mod 97，当 y=0 时自然得出 x。

**逆元关系也不是显式编码的**——模型计算 (x + (97-x)) mod 97 = 0，所有逆元对被映射到同一个"答案 0 方向"，是计算的结果而不是记忆的模式。

## 总结

模型用两步完成 mod 97 加法：
1. **Block 0**：利用 K 和 V 投影放大 embedding 中的环形拓扑，建立 attention 模式
2. **Block 1**：执行实际计算——将 `=` 位置的 128 维隐藏状态精确旋转到对应答案的方向

单位元（x+0=x）和逆元（x+(97-x)=0）**不是被显式记忆或特殊编码的**。它们是统一的加法算法的自然推论——模型学到了一个通用的"在环上旋转并读出位置"的计算过程，当输入恰好是 0 或逆元对时，算法自然给出正确答案。

## 对稀疏化的启示

1. **稀疏化保留核心计算层的结构**：block0 的 K 和 V 投影中的环形结构在 L0=0.40 下保持良好（下降约 30%），因为这是模型执行加法的关键路径。
2. **稀疏化清除冗余层的结构**：FFN_up_block0 的环形信号从 0.321 降到 0.045——这说明 dense 模型中 FFN 的环形是冗余的，不是执行计算必需的。
3. **U 形 ratio 曲线反映编码效率**：最优稀疏度下模型保留核心路径（K、V），清除冗余路径（FFN、block1 的 K），用最少的有效连接实现计算。
4. **Block 1 是关键层**：激活分析显示计算主要发生在 block 1。稀疏化如果过度损伤 block 1 的权重，模型将无法完成加法。
5. **可测试的预测**：改做 `x*y mod 97`，预期 K 层会编码乘法群结构而非环形加法结构。

## 复现

```bash
# Output_proj 层的详细代数分析（单层，带可视化）
python experiments/superposition/analyze_algebra.py

# 全层对比分析（12 层，带汇总图表）
python experiments/superposition/analyze_algebra_full.py

# 激活分析（单位元、逆元、答案编码）
python experiments/superposition/analyze_activations.py
```

输出目录：`experiments/superposition/results/geometry/algebraic_structure/`
- `token_similarity_*.png`：97x97 余弦相似度矩阵热力图
- `algebraic_structure_*.png`：相似度 vs (i+j) mod 97 的柱状图 + 分布直方图
- `dlog_reordered_*.png`：按离散对数重排后的相似度矩阵
- `neighborhood_all_layers_*.png`：各层邻居相似度对比
- `inverse_signal_*.png`：各层逆元信号强度对比
